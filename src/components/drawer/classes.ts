import { createClasses } from 'src/theme/create-classes'

// ----------------------------------------------------------------------

export const drawerClasses = {
  root: createClasses('drawer__root')
}
